#include "main.h"

/**
 * mul takes two values
 * mulltiply them and returns the value
 * Return: presents the value
 */
int mul(int a, int b)
{
	int results ;

	results = a * b;
	return (results);
}
